# CrudDude
A simple and robust CRUD (Create, Read, Update, Delete) REST API built with Node.js and Express.js. Designed for managing data efficiently, with full support for HTTP methods, validation, and error handling.
